<Window x:Class="Restaurant.EmplPages.EditEmployeeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant.EmplPages"
        mc:Ignorable="d"
        Title="EditEmployeeWindow" Width="347">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,10,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#80BDFF"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="TitleText"
                   Text="НОВЫЙ СОТРУДНИК" 
                   FontSize="20" FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>

        <Border Grid.Row="1" 
                Width="100" Height="100"
                Background="#F0F0F0" 
                CornerRadius="50"
                Margin="0,0,0,20"
                BorderBrush="#DEE2E6"
                BorderThickness="2"
                HorizontalAlignment="Center">
            <Image x:Name="EmployeeIcon"
                   Stretch="Uniform">
                <Image.Style>
                    <Style TargetType="Image">
                        <Setter Property="Source" Value="/img/icons/user.png"/>
                    </Style>
                </Image.Style>
            </Image>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Фамилия:" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Grid.Column="1" x:Name="LNameBox"
                     Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FieldTextBoxStyle}"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Имя:" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Grid.Column="1" x:Name="FNameBox"
                     Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FieldTextBoxStyle}"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Отчество:" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Grid.Column="1" x:Name="MNameBox"
                     Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FieldTextBoxStyle}"/>
        </Grid>

        <Grid Grid.Row="5" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Должность:" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Positions}"
                      SelectedItem="{Binding SelectedPosition}"
                      DisplayMemberPath="PositionName"
                      FontSize="14" Padding="5"
                      Margin="0,0,0,15"/>
            <Button Grid.Column="2" 
                Content="📋" 
                Width="30" Height="30"
                Margin="5,0,0,0"
                Click="PositionsBtn_Click"
                ToolTip="Управление должностями"
                Style="{StaticResource IconButtonStyle}"/>
        </Grid>

        <Grid Grid.Row="6" Margin="0,0,0,10" 
              Visibility="{Binding IsNewEmployee, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="138"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Пригласительный код:" 
                       Style="{StaticResource FieldLabelStyle}" Height="16" Margin="0,0,10,0"/>
            <TextBox x:Name="InviteCodeBox"
                     Text="{Binding InviteCode, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     Style="{StaticResource FieldTextBoxStyle}"
                     IsReadOnly="True"
                     Margin="0,0,38,15" Grid.Column="1"/>
            <Button Grid.Column="1" 
                    Content="🎲"
                    Height="NaN"
                    Click="GenerateInviteCode_Click"
                    Style="{StaticResource SecondaryButtonStyle}" Margin="125,12,0,20"/>
        </Grid>

        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveBtn" 
                    Content="💾 Сохранить" 
                    Width="120" Height="35"
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Click="SaveBtn_Click"/>
            <Button x:Name="CancelBtn" 
                    Content="Отмена" 
                    Width="100" Height="35" 
                    Margin="10,0,0,0"
                    Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
